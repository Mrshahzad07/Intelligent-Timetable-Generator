# Mandatory AI Usage Report

**AI TOOL USED:**  
Gemini / Antigravity IDE

---

### WHAT I ASKED AI TO DO:
1. **Develop an Intelligent Constraint Satisfaction (CSP) Timetable Solver**: Formulate the college timetable problem mathematically and build a hybrid CSP solver implementing Minimum Remaining Values (MRV) and Degree heuristics with forward checking and soft constraint optimization.
2. **Engineer an Impossibility & Conflict Diagnostic System**: Build a pre-flight feasibility engine based on the pigeonhole principle, room capacity bounds, and teacher contract limits that informs users of impossible or conflicting constraints with plain-English diagnostics and 1-click auto-fix actions.
3. **Build an Institutional Multi-View Interactive Grid**: Develop a complete web interface supporting Division, Faculty, Classroom, and Master matrix views, real-time conflict-aware drag-and-drop/manual rescheduling, and CSV/JSON export.
4. **Fix Layout Screen Ratios and Responsive Micro-Interactions**: Prevent slot timing text overlap across varying viewport aspect ratios, implement sticky headers for horizontal scrolling, and create high-end card hover micro-animations (3D elevation, glassmorphism shimmer, spotlight search glow) across dark ("mortem") and light themes.
5. **Architect an Institutional A4 Landscape Print & PDF Studio**: Create an official academic timetable printing engine conforming to physical paper standards, complete with college letterhead, customizable sections (Course Directory, Facility Legend, Academic Regulations, 3-Tier Signatures), and hardened `@media print` CSS.
6. **Implement an Autonomous AI Ingestion Agent & Evolutionary Genetic Solver**: Eliminate static dummy data dependencies by building a 100% dynamic input flow. Implement an AI Agent modal capable of parsing free-form natural language curriculum prompts, a 5-step guided interactive interview wizard, an empty state control center, and a Genetic Algorithm (GA) evolutionary optimizer (`src/solver/geneticAlgorithmSolver.js`) that performs chromosome mutation and selection across generations.

---

### PROMPT THAT WAS MOST USEFUL:
> *"In this you already have added so many dummy datas so I want you to remove all the dummy datas and make it dynamic so make it a genetic like that so agent will generate the timetable as per the given data user or admin or professor or any principle charity will provide the data based on their data it will generate the timetable and it will show accordingly so remove all the dummy data make it as behave real and dynamic so it would be proper in used Super make it like that so agent will give ask all the things and agent agent will AI agent will generate so it should be intelligent timetable generator like AI based AI intelligence based timetable chain data amount to build it okay so build it accordingly"*

---

### CODE GENERATED BY AI: What part?
- **Algorithmic Engine & Optimization**:
  - `src/solver/timetableSolver.js`: CSP backtracking search engine with MRV/Degree variable ordering, LCV value selection, forward checking, and domain pruning.
  - `src/solver/feasibilityChecker.js`: Pre-flight algebraic bounds analyzer testing pigeonhole limits, lab capacity bottlenecks, and faculty contract limits.
  - `src/solver/geneticAlgorithmSolver.js`: Population-based evolutionary optimizer using chromosome encoding, tournament selection, single-point crossover, and constraint-preserving random gene mutation with live generation progress reporting.
  - `src/solver/fitnessEvaluator.js`: Multi-objective penalty/reward fitness scoring function evaluating subject spread, faculty rest gaps, workload ceilings, and room utilization.
  - `src/solver/conflictValidator.js`: Real-time interactive validator preventing double-bookings and constraint breaches during manual moves.
- **Dynamic AI Ingestion & User Experience**:
  - `src/components/AIAgentAssistantModal.jsx`: Natural language prompt extractor, curriculum regex tokenizer, real-world department presets, and 5-step interactive interview wizard.
  - `src/components/EmptyStateControlCenter.jsx`: Zero-dummy-data onboarding canvas with instant AI agent launch, manual custom builder, and quick-start department templates.
- **Institutional Print & Visual Presentation**:
  - `src/components/PrintSheetModal.jsx` & `src/components/PrintableTimetableSheet.jsx`: Configurable A4 landscape print preview with letterhead customization and toggleable legend/regulation sections.
  - `src/components/AnalyticsModal.jsx`: Institutional audit modal displaying hard invariant validation, room utilization, and fitness distribution.
  - `src/App.css` & `src/index.css`: Dual-theme design system (Cybernetic Dark `#090d16` & Crisp Daylight), card hover micro-animations (`@keyframes shimmerSweep`), and `@media print` CSS rules.

---

### CODE I MODIFIED: What part?
1. **Data Models & Schema Completeness (`src/data/models.js`)**:
   Expanded the initial constants into an end-to-end domain model with entity factory functions (`createDivision`, `createRoom`, `createFaculty`, `createSubject`), entity validation routines, and explicit constraint constants (`HARD_CONSTRAINTS`, `SOFT_CONSTRAINTS`).
2. **Node.js ESM Compatibility for Test Automation**:
   Updated all internal import statements across `src/solver/` and `src/data/` to include explicit `.js` extensions, enabling the automated verification suite (`tests/verifySolver.js`) to execute natively in Node.js 22.
3. **Empty State & Dynamic Ingestion Wiring (`src/App.jsx`)**:
   Connected the blank canvas empty state (`EmptyStateControlCenter.jsx`) to conditionally render when zero timetable data is present, integrated the Genetic Algorithm solver as an alternative generation pipeline with progress toasts, and unified both solver engines into the shared state.
4. **Print Layout Geometry & Responsive Typography (`src/App.css`)**:
   Adjusted `@page { size: A4 landscape; margin: 8mm; }`, calibrated table column widths to prevent page overflow, and created clean high-contrast black/white styling for physical printing while preserving full-color UI themes on screen.

---

### AI OUTPUT THAT WAS WRONG:
1. **Extensionless Relative Module Imports**:
   Initial files generated imported relative modules without file extensions (e.g., `import { ROOM_TYPES } from './models'`). While Vite bundler resolves these automatically in browser environments, native Node.js ESM execution failed with `ERR_MODULE_NOT_FOUND`.
2. **Missing Blank Canvas / Zero-Entity Handling**:
   The initial solver implementation assumed that `divisions`, `rooms`, and `faculty` arrays were always populated with demo data. When user cleared data or started from a blank canvas, calling `checkFeasibility` or `generateTimetable` without data caused `Cannot read properties of undefined (reading 'length')` or returned division overflow errors on 0 active periods.
3. **Print Layout Clipped in Browser Print Preview**:
   Initial `@media print` rules did not enforce explicit landscape orientation or hide fixed navigation elements, causing browser print engines to render portrait-oriented cuts and include interactive UI buttons on paper.

---

### HOW I IDENTIFIED THE PROBLEM:
1. Running `node tests/verifySolver.js` in the terminal immediately produced:
   ```text
   Error [ERR_MODULE_NOT_FOUND]: Cannot find module 'D:\AI-Timetable-generator\src\data\models' imported from D:\AI-Timetable-generator\src\data\presets.js
   ```
2. Testing the "Clear All Data" and "Blank Canvas" workflow in the browser caused the feasibility drawer to flag an unexpected division overflow error because 0 slots available were compared against 0 sessions using unhandled division edge conditions.
3. Launching `window.print()` in Chrome browser inspection revealed table headers splitting across multiple pages and the web navbar bleeding into the printable document.

---

### HOW I FIXED IT:
1. **Added `.js` Extensions**: Enforced explicit `.js` paths across all solver and preset import statements (`import { ROOM_TYPES } from './models.js'`).
2. **Engineered Blank Canvas Guards**: Updated `feasibilityChecker.js` and `timetableSolver.js` to check for zero-entity inputs upfront (`if (!divisions || divisions.length === 0)`), gracefully returning an empty state object with descriptive guidance instead of throwing runtime exceptions.
3. **Hardened Print Engine CSS**: Added dedicated `@page { size: A4 landscape; margin: 8mm; }`, isolated the printable sheet container using CSS classes, set `overflow: visible` during printing, hid all non-print interface controls (`nav`, `.app-header`, `.analytics-modal`, `.drawer`), and added explicit cell border rules.
4. **Automated Regression Suite**: Maintained and verified the 20-test automated verification suite (`npm test`), achieving a 100% pass rate across feasibility detection, invariant preservation, and conflict rejection.
